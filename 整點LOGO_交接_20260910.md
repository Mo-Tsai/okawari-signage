# 整點 LOGO 跑燈・交接　2026-09-10（四）
> **2026-09-11 追加：台南關屏改 22:10。** 見最後一節。

> 台南現場要用的。出門前看這一份就好。

---

## 一句話

門頭屏每個整點過 **50 分**會換成品牌綠底，OKAWARI 的 LOGO 跑過去，一分鐘後回到原本的畫面。
兩家店都做好了，內容已經打包進 `現場包/`。
台南一天 12 次（關屏 22:10），台中一天 13 次（關屏 23:00）。

給業主看的網頁：**https://mo-tsai.github.io/okawari-signage/logo.html**
（同一份也在 `docs/logo.html`，影片包在檔案裡，可以直接用 LINE 傳，對方不用連網。）

---

## 明天的動作

### 台南（你人在現場）

**用筆電從後台發佈，不要用隨身碟那支 exe。**

> 卡 IP、路由器 SSID 和密碼查 `讀我.md` 的「兩家店的差異」那張表（那份沒進 git）。

```bash
cd 後台
python _發佈到卡.py tainan <卡IP>
python _送開關屏排程.py tainan <卡IP>
python 看一天.py tainan <卡IP>
```

第一支會**把新的節目 guid 寫回 `stores.json`**，第三支才驗得動。
用 `灌內容_v2.exe` 的話畫面一樣會對，但驗收工具會全部失效（見下面那一節）。

`看一天.py` 現在會演 17 支，其中一支就是整點 LOGO。約三分鐘。

### 台中（店員按隨身碟）

你選的是「照灌，接受按鈕先死」。灌完之後台中屏上會是：

| | |
|---|---|
| 六段時段畫面 | ✅ 照常 |
| 整點 LOGO 跑燈 | ✅ 每小時一次 |
| 整點彩蛋、開幕檔期、櫃檯三顆按鈕 | ❌ **全停** |

**原因**：`灌內容_v2.exe` 每灌一次，卡上每支節目都會拿到新的 guid，
但它不會把新 guid 寫回任何地方，也碰不到櫃檯那台 M5。
M5 手上還是舊 guid → 送 SwitchProgram 卡就拒絕。

**壞掉的樣子是「按了沒反應」，不是黑屏。** 畫面完全正常，所以現場看不出來。
先跟店員說一聲，不然他們會以為按鈕機壞了。

**之後怎麼救**（要有人帶筆電到台中，十分鐘）：

```bash
cd 後台
python _發佈到卡.py taichung <卡IP>
python _送開關屏排程.py taichung <卡IP>
cd ../按鈕機
python 做設定檔.py taichung --router <SSID> <密碼> <卡IP>
python 送設定到M5.py <M5的IP>
```

---

## ★ 一個一直都在、今天才查出來的坑

**只要用 `灌內容_v2.exe` 灌過，隨身碟上的 `試跑一次_v1.exe` 就驗不動了。**

它是從隨身碟的 `stores.json` 讀節目 guid 的，而 `灌內容` 灌完會換一批新 guid、
不會寫回去。所以它會把 17 支**全部**報成「沒切過去」——
而屏上的畫面其實是好的。

這不是這次改出來的，是 9/9 台南重灌之後就已經這樣了，只是沒人回頭驗過那支。

**現在的辦法**：灌完要驗收就用筆電跑 `後台/看一天.py`，
它讀的是 `後台/stores.json`，而 `_發佈到卡.py` 會把 guid 寫回那裡。

**之後要修**：讓 `試跑一次` 改成跟卡要現在的節目表（`GetProgram`），
用檔名對回 key，不要信隨身碟上那份。原始碼已經改到 v2 但**還沒重新打包成 exe**
（沒有 build 腳本，也沒有卡可以測，不在出門前做）。

---

## 這次改了什麼

| 檔案 | 改了什麼 |
|---|---|
| `後台/scene_logo.py` | **新**。綠底跑燈：滑進來 → 停住掃一道亮帶 → 滑出去 |
| `後台/素材/okawari_logo.png` | **新**。業主 LOGO 的複本 |
| `後台/_排整點LOGO.py` | **新**。在時段表上每小時挖一分鐘。可重複跑，跑完自己驗 |
| `後台/_出LOGO預覽網頁.py` | **新**。產生 `docs/logo.html` |
| `後台/artwork.py` | 掛上 logo，加 `logo_*` 參數 |
| `後台/_一天的內容.py` | 秒數查不到時回頭問美術參數（不然表格會印錯的 10 秒） |
| `後台/看一天.py` | 演出清單改成**照時間挑節目**，不再硬綁 key；加了一支 LOGO |
| `試跑一次_門頭屏.py` | 同上（原始碼 v2，**exe 還是 v1**） |
| `按鈕機/做設定檔.py` | LOGO 不算「時段畫面」，M5 不能拿它當回歸點 |
| `按鈕機/main.py` | 午睡判斷改前綴比對（siesta 的窗被切成好幾段了） |

卡上的節目 **24 → 50 支**，影片只多一支（13 格 LOGO 共用同一份）。

---

## 為什麼是「挖掉一分鐘」，不是「多疊一支節目上去」

架構 B 下時段畫面是 `count=999`：一支播完接著播自己，五個半小時，
**在時段結束前卡永遠輪不到任何別的節目**。整點彩蛋和開幕檔期就是這樣被犧牲、
改由櫃檯 M5 主動送的。

所以 LOGO 如果只是「多加一支有時段的節目」，它**永遠不會出現**。
唯一能讓卡自己放的辦法，是把那一分鐘從時段畫面的窗裡挖掉 ——
時段畫面在那一分鐘沒資格，卡就只剩 LOGO 可播。

**好處是它不靠 M5。** 台南根本還沒裝按鈕機（所以那面屏從來沒放過彩蛋和檔期），
LOGO 走這條路兩家店都會動，店員重灌之後也還在。

**代價**是時段畫面被切成好幾段（`noon` → `noon` / `noon_2` / `noon_3` / `noon_4`），
而任何「key 寫死、時間寫死」的工具都會對不上 —— 上面 `看一天.py` 和
`試跑一次` 那兩筆修改就是為了這件事。**以後再挖窗要記得回頭看這兩支。**

---

## 要改的時候

```bash
cd 後台
python _排整點LOGO.py tainan taichung --分 40   # 換一個分鐘
python _排整點LOGO.py tainan taichung --拿掉    # 整個拿掉，還原
python _建全部.py tainan                        # 重編影片
python _打包現場包.py                           # 重新打包隨身碟
python _出LOGO預覽網頁.py                       # 更新給業主看的網頁
```

`_排整點LOGO.py` 跑完會自己驗一遍：開屏的 45000 秒裡，每一秒都必須剛好一支
時段畫面有資格。0 支代表卡會去輪到 COMBO（8/17 那次事故），2 支代表行為不可預測。
驗不過它不會寫回 `stores.json`。

**分鐘不要挑 :00、:15、:30、:45** —— 那四個是驗收工具用來假裝時間的刻度，
而 M5 也在 :00 和 :30 插播彩蛋。目前用 :50。

跑燈本身的手感在 `後台/artwork.py` 的 `logo_*` 參數：
`logo_seconds`（一遍幾秒）、`logo_hold`（停在中間多久，0 = 純跑燈不停）、
`logo_height`（佔畫面多高）、`logo_shine`（亮帶寬度，0 = 不掃）。


---

# 追加・2026-09-11：台南關屏提前到 22:10

百貨規定 **22:10 一定要關掉**。這是硬牆，其他時間都往它退。

原本的設計是「提前 30 分開屏、延後 30 分關屏」，前後各半小時播睡覺畫面。
要守住那個對稱又要 22:10 關屏，**賣飯時間就得收在 21:40**（原本 22:30）。
開屏維持 10:30 不動 —— 那半小時的用途是「開店前有 30 分鐘反應問題」，跟關屏無關。

## 台南新的一天

| | 舊 | 新 |
|---|---|---|
| 屏開 | 10:30 – 23:00 | 10:30 – **22:10** |
| 晚間（今天辛苦了） | 17:30 – 22:30 | 17:30 – **21:40** |
| 打烊後・睡了 | 22:30 – 23:00 | **21:40 – 22:10** |
| 買一送一、溫泉蛋的時間窗 | 到 22:30 | 到 **21:40** |
| 櫃檯三顆按鈕可按的時間 | 10:30 – 23:00 | 10:30 – **22:10** |
| 整點 LOGO | 13 次（到 22:50） | **12 次**（到 21:50） |
| 卡上的節目 | 50 支 | **48 支** |

**台中完全不受影響**，還是 10:30–23:00、一天 13 次。
從今天起兩家店的關屏時間不一樣了，別再假設它們一樣。

## ★ 灌完一定要另外送開關屏排程

**這一步是這次改動的重點，漏了等於沒改。**

`_發佈到卡.py` 不會寫開關屏排程 —— 節目會照新時段播，
但**卡還是會亮到 23:00**，百貨那邊就過不了。

```bash
cd 後台
python _發佈到卡.py tainan <卡IP>
python _送開關屏排程.py tainan <卡IP>     # ★ 這一支才是 22:10 關屏
python 看一天.py tainan <卡IP>
```

送完回頭確認卡真的收到了：

```bash
python _對時.py <卡IP> --check
```

（用隨身碟的 `灌內容_v2.exe` 的話它自己會寫開關屏排程，而且會讀回來對，
所以那條路不會漏。但它會讓櫃檯按鈕失效 —— 台南沒裝 M5，所以台南沒差。）

## 一個留著沒動的東西

`egg_22`（整點彩蛋 22:00）現在落在「打烊後・睡了」那半小時裡。
台南沒有 M5，彩蛋根本不會放，所以今天不影響任何事 ——
**但哪天台南裝了按鈕機，要先決定這支是關掉還是留著。**
