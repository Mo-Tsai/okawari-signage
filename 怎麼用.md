# 怎麼用

> 只講操作。原理和遠端維運的推導在 `遠端架構.md`，急著用不必看。

網址：**https://mo-tsai.github.io/okawari-signage/**

---

## 先搞懂一件事，其他都好懂

**網站只是遙控器。真正在跟 LED 卡講話的是你筆電上的後台。**

```
   網站（GitHub）              筆電（後台.bat）           控制卡 → LED 屏
   按鈕在這裡      ───────▶   出力的在這裡    ───網路線───▶
```

所以**筆電一定要開著、一定要接著卡**。網站只是換個地方按按鈕。

---

## 一、平常要用的時候（三步）

### 1. 筆電接上卡

網路線插上。或者連卡自己的 Wi-Fi 熱點也可以。

### 2. 開後台

雙擊 `後台/後台.bat`。跳出黑色視窗、印出一堆網址就是好了。**這個視窗不要關。**

### 3. 開網站

在**筆電上**打開 https://mo-tsai.github.io/okawari-signage/

右上角應該亮綠燈。沒有的話按「連線設定」→ 按「本機」→ 按「套用」。

然後：**選門店 → 按「連線到卡」→ 按「發佈到卡」→ 按三個大按鈕**。

就這樣。

---

## 二、手機和平板要怎麼連（★ 這裡有個坑）

**手機不要用上面那個 GitHub 網址。** 要用這個：

```
http://192.168.50.229:8080
```

（`192.168.50.229` 是你筆電的 IP。開後台的時候黑視窗會印出來，
換了 Wi-Fi 就會變，以印出來的為準。）

**為什麼不能用 GitHub 網址？**
GitHub 網站是 `https` 的，瀏覽器規定 https 的頁面只能連 `localhost`，
不准連 `192.168.x.x`。這是瀏覽器的安全規則，改不了。

所以記住兩句話就好：

| 誰 | 開哪個網址 |
|---|---|
| **筆電自己** | https://mo-tsai.github.io/okawari-signage/ |
| **手機、平板**（同一個 Wi-Fi） | http://筆電的IP:8080 |

**兩個網址打開來是一模一樣的畫面**，因為後台會把 GitHub 上那一份同樣端出來。
所以不用記兩套操作。

---

## 三、去現場的時候

**帶筆電去。** 到了現場：

1. 找個 Wi-Fi 給筆電（店裡的、或你手機開熱點都行）
2. 筆電用網路線接上卡
3. 開 `後台.bat`
4. 筆電上開 GitHub 網址操作
5. 要用手機跑來跑去看畫面的話，手機連**同一個 Wi-Fi**，開筆電的 IP

第 1 步的 Wi-Fi 只是為了讓手機跟筆電看得到彼此。**發佈內容不需要外網** ——
影片是筆電自己編的，走網路線送進卡裡。就算完全沒有網路，
筆電直接接卡也能發佈，只是手機連不進來而已。

> **連不到卡？** 跑
> `_明天帶去_LED實測_20260810/02_程式端/現場連線.bat`，
> 它會自己掃描、自己當 DHCP 發 IP 給卡。

---

## 四、我改了東西，怎麼更新網站

改完 `docs/index.html` 或 `後台/` 裡的程式之後，
雙擊根目錄的 **`推上GitHub.bat`**，打一句改了什麼，按 Enter。

大約一分鐘後網站就更新了。**不用重新設定任何東西**，
你和所有人開的都是最新版。

---

## 五、要注意的兩件小事

**1. `stores.json` 不會上傳。**
那裡面有卡的 MAC 和熱點密碼，所以刻意排除掉了。
換一台電腦跑的話，把 `後台/stores.範本.json` 複製成 `stores.json` 再填。

**2. 卡的熱點密碼還是出廠的 `88888888`。**
正式掛上店門口之前記得改掉 —— 不然走在騎樓下的人就能連上去改畫面。

---

## 出問題的時候，照這個順序看

| 症狀 | 先看這個 |
|---|---|
| 網站右上角紅燈、說「後台未連線」 | 後台的黑視窗還開著嗎？ |
| 手機打不開 | 用的是**筆電的 IP**，不是 GitHub 網址？同一個 Wi-Fi？ |
| 按「連線到卡」失敗 | 網路線插了嗎？跑 `現場連線.bat` |
| 按了播放但螢幕沒反應 | 先按「發佈到卡」。內容沒發佈上去就切不了 |
| 「抓屏上實際畫面」是黑的 | **正常。**卡的影片畫在硬體層，截不到圖，不是壞掉 |
| 網站改了但看起來沒變 | 等一分鐘，然後在瀏覽器按 Ctrl+F5 強制重新整理 |
