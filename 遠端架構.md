# 遠端怎麼控？

> 寫給未來的自己。看完應該要能回答一件事：
> **裝上去之後，我人不在現場，還控不控得到？**

---

## 一句話的結論

**遠端控得到，已經在測試卡上實測過。**
但「我改內容」和「店員按續碗」要走兩條不同的路，
所以門市還是建議放一台小主機。細節在下面。

---

## 先講三件會決定所有事的事實

### 一、外面連不進 4G

台灣的 4G SIM 給的是電信商的大 NAT（CGNAT，100.64.x.x 那種）。
那不是一個「在網際網路上找得到」的位址。沒有公網 IP，就沒有埠轉發可以設，
路由器裡翻遍所有選單也不會有。

所以**「我從家裡連到店裡的卡」這條路，在 4G 上是結構性走不通的**，
不是設定沒調對、也不是再試一次就會通。有線寬頻好一點，但門市的數據機
通常是房東或電信商裝的，我們動不到，而且 IP 會變。

**結論：方向要反過來。要嘛店裡的東西主動撥出來，要嘛什麼都不會通。**

### 二、卡自己會撥出去

SDK 有這支：

```xml
<in method="SetSDKTcpServer">
  <server port="6000" host="hq.example.com"/>
</in>
```

文件寫得很清楚：「用于需要设备向tcp服务器进行连接, 而不是主动向设备连接的功能」。
設好之後換卡主動連我們，連上之後協議角色不變 —— 還是我們送 `<in>`、卡回 `<out>`。

出站連線在 CGNAT 底下永遠是通的。所以這一支就是解法。

#### ★ 2026-08-10 已經在那張測試卡上實測過了，會動

不是查文件推論的，是真的跑過一遍。結果：

| 測的東西 | 結果 |
|---|---|
| 設定完之後卡會不會撥回來 | **會。10〜15 秒之內就撥回來了** |
| 撥回來之後能不能下指令 | **能。**`SetLuminancePloy` 回 `kSuccess` |
| 把後台關掉 60 秒再開，卡會不會自己重撥 | **會，但要等。約 2〜3 分鐘一次** |
| 中間全部斷光之後，卡上的設定還在嗎 | **在。**再問一次 `GetSDKTcpServer` 還是我們設的值 |

（測完已經把卡還原成 `host="" port="0"`，跟原本一樣。）

**所以事實二成立：4G 底下遠端控得到。** 這是這份文件最重要的一句話。

還剩一題沒測，因為要動到電源：

| 還不知道 | 怎麼測 |
|---|---|
| 卡**斷電重開**之後，設定還在嗎？ | 拔卡的電源再插回去，等三分鐘看它撥不撥回來 |

設定在斷線之後都還留著，所以它應該是寫在 flash 裡的，斷電多半也還在。
但這一題便宜又致命 —— 下次碰到卡的時候順手做掉。

#### 那個「2〜3 分鐘」要記住

重撥間隔 2〜3 分鐘，對兩件事的意義完全不一樣：

- **改內容**：無所謂。反正我也不是秒級在改。
- **店員按續碗**：**完全不能接受。** 客人站在旁邊，不可能等三分鐘。

這就是下一節要講的事。

### 三、網路斷掉，螢幕不會黑

這件事要先講，因為它把最壞情況的等級整個往下拉。

內容是存在卡的 SD 卡上的，卡自己播。網路斷掉 = **我今天改不了內容**，
不等於門口沒畫面。畫面會照常跑。

所以真正的風險是「更新不了」，不是「開天窗」。這是可以忍幾天的等級。

---

## 但有兩件事的需求是相反的

把要做的事拆開看，會發現它們根本不該走同一條路：

| | 內容更新 | 現場觸發續碗 |
|---|---|---|
| 誰做 | 我 | 店員 |
| 多常 | 一個月幾次 | 一天幾十次 |
| 慢一點行嗎 | 行 | 不行，客人在旁邊 |
| 網路斷了呢 | 等明天再說 | **不能停** |
| 從哪來 | 外網 | 應該要在店內 |

**續碗的觸發不能靠外網。**
如果店員按一下要繞到台南總部再繞回來，4G 一抖就按不動了，
而那是有客人站在旁邊的時刻。

這一條就決定了架構。

---

## 建議做法：門市放一台小主機

```
   我（任何地方）
     │
     ├── GitHub Pages ────────── 介面（這頁只是介面，我推更新）
     │
     └── https://tainan.xxx ──┐
                              │  Cloudflare Tunnel（小主機主動撥出，不需要公網 IP）
     ┌────────────────────────┘
     ▼
  ┌─────────────────────────────────────────┐
  │ 店內                                     │
  │                                          │
  │  [小主機] ──網路線──▶ [控制卡] ──▶ LED屏 │
  │      ▲                                   │
  │      └── 店內 Wi-Fi ── [平板] 店員按續碗 │
  └─────────────────────────────────────────┘
```

小主機就是現在筆電在做的事，只是留在店裡。跑的是同一套 `後台/`。

**兩條路各自獨立到同一個後台。** 這是重點：

- 店員那條線**從頭到尾沒有離開店裡**。外網死掉、4G 停機、Cloudflare 掛掉，
  續碗照按，畫面照跑。
- 我那條線走隧道進來。斷了就是我今天改不了東西，僅此而已。

順帶解決的問題：

- **影片不用過 4G。** 編譯在店裡做，20 MB 的 mp4 走的是店內網路線。
  不然每次發佈都要從外面推幾十 MB 上去，慢又貴。
- **不必依賴卡的韌體行為。** 重連是 cloudflared 的事，那東西很成熟。
- **出事可以直接進去看。** 遠端桌面或 SSH，不必請人幫忙唸螢幕上的字。

代價：

| | |
|---|---|
| 小主機 | 一台 NT$3,000〜6,000，二手 mini PC 就夠。要能跑 Python + ffmpeg |
| Cloudflare Tunnel | 免費 |
| GitHub Pages | 免費 |
| 風險 | 多一個會被清潔阿姨拔掉的插頭。貼標籤：「請勿拔除 · 門頭屏」 |

### 沒有小主機的版本（備案）

如果就是不想在門市放電腦，那就走事實二：卡自己撥回總部。

```
  卡 ──4G──▶ 台南總部一台固定機器（或月租 VPS）◀── 我的瀏覽器
```

後台已經寫好了。啟動前設一個環境變數就會開始聽：

```bash
set OKAWARI_HQ_PORT=6000
```

然後在卡連得到的時候，用後台的 `/api/hq` 把卡指向總部位址。

這條路已經驗證過會動了（見事實二）。但要接受：

- **續碗變成要繞外網，而且斷線後要等 2〜3 分鐘才會恢復。**
  這是這個方案真正的痛點 —— 不是技術問題，是客人站在旁邊的那三分鐘。
- 影片要從總部推到門市，走 4G。每次發佈幾十 MB。
- 卡斷電重開後設定還在不在，還沒測。

> **所以：能放小主機就放。**
> 備案是給「先開幕再說」的過渡期用的，或者給那種「續碗改成整點自動觸發、
> 不靠店員按」的簡化版本 —— 那樣就沒有即時性的問題，備案完全夠用。

---

## 你提的兩個方案，我的看法

### 「靠現場服務人員或 iPad 連卡的熱點」

**留著當救命用，但它不能當成日常的操作方式。** 原因是你自己已經講到一半的那個：

iPad 連上卡的 AP（192.168.6.1）之後，**iPad 就沒有網路了**。
它連得到卡，但連不到後台。而且就算連得到 ——
**iPad 跑不了 ffmpeg 和 Python**，內容是在後台編出來的，不是在瀏覽器裡。

> 連上卡的熱點，拿到的是「那張卡」，不是「整套系統」。

所以救援包裡該放的不是 iPad，是**一台裝好東西的筆電**：
Python + Pillow + ffmpeg + 這個資料夾。它連上卡的 AP 就是一套完整的離線系統，
編譯、送檔、切節目全部能做。這才叫救援。

那 iPad 有什麼用？如果哪天真的需要，可以再做一支只會「切節目」的極簡頁面
（節目已經在卡上，只要送 SwitchProgram），讓店員在完全沒有外網的情況下
至少能手動切畫面。但那需要有東西能講卡的二進位 TCP 協議 —— 瀏覽器不行，
還是得有一台跑 Python 的機器。**結論一樣：關鍵是那台機器，不是那台平板。**

### 「我的 Wi-Fi 連過去改後台」

這其實就是小主機方案，只是用筆電扮演小主機，而且要我人在現場。
裝機當天完全正確，就這樣做。日常維運不行 —— 總不能為了改一張圖跑去台中。

---

## 那我到底該怎麼確定「控得到」

**不要等到裝機當天才第一次試。** 下面三個測試，前兩個在辦公室就能做完，
用現在這張測試卡。

### 測試一 · 卡會不會自己撥回來 —— ✅ 2026-08-10 做完了

結果寫在上面事實二那一節。要重現的話：

1. 後台加環境變數啟動：
   ```
   set OKAWARI_HQ_PORT=6000
   後台.bat
   ```
2. 用平常的方式連上卡，然後把卡指向這台機器：
   ```bash
   curl -X POST http://localhost:8080/api/hq -H "Content-Type: application/json" -d "{\"host\":\"筆電的IP\",\"port\":6000}"
   ```
3. 後台視窗會跳出「★ 卡從 xxx 撥回來了」，大約 10〜15 秒

不玩了要還原：把 host 設回空字串、port 設 0。

**只剩斷電那一題**：拔掉卡的電源再插回去，等三分鐘，看它自己撥不撥回來。
下次碰到卡的時候順手做掉。

### 測試二 · 用今天有的東西，把整條遠端路線先跑一遍 ★

這個測試很值得做，因為它**不需要買任何東西、不需要去現場**，
就能把上線後的完整拓樸演練一次。

1. 筆電裝 cloudflared（`winget install Cloudflare.cloudflared`）
2. 後台照常跑著，另開一個視窗：
   ```bash
   cloudflared tunnel --url http://localhost:8080
   ```
   它會吐一個 `https://隨機字.trycloudflare.com` 給你
3. **手機關掉 Wi-Fi，只用 4G**，開 GitHub Pages 上的那個介面
4. 「連線設定」填剛剛那個 https 網址
5. 按發佈、按續碗

如果這樣能動 —— 手機在 4G 上、卡在辦公室網路裡、介面在 GitHub 上 ——
**那條遠端路線就已經證明過了**，剩下的只是把筆電換成門市的小主機。

（`trycloudflare.com` 的網址每次重跑都會變，正式上線要註冊一個
Cloudflare 帳號綁固定網域。免費。）

### 測試三 · 斷網演練

裝好之後、離開現場之前，當著自己的面做一次：

1. 拔掉小主機的對外網路（或關掉 4G 路由器）
2. **確認螢幕還在播** ← 這是最重要的一格
3. 用店內 Wi-Fi 的平板按續碗，**確認還能觸發**
4. 插回去，什麼都不要碰，**看它自己會不會恢復遠端**

第 4 步是這整份文件真正要回答的問題。如果它自己會回來，就可以放心走。
如果要人手動做什麼才會回來，那就要在離開之前把「那件事」寫成一張紙貼在牆上。

---

## 裝機當天，走之前的檢查表

**下面每一項都打勾才能離開現場。**

- [ ] 讀到卡的 `device_id`，填進 `stores.json`
- [ ] 用**手機的 4G**（不是店裡的 Wi-Fi）打得開介面，而且發佈成功
- [ ] 小主機、控制卡、路由器**全部一起斷電再開**，五分鐘內自己恢復
- [ ] 店員的平板按續碗會動，而且是走店內網路
- [ ] 螢幕亮度現場看過（白天、晚上各看一次）
- [ ] 小主機設成開機自動啟動後台
- [ ] 插頭貼標籤
- [ ] 手上有店長電話，牆上有一張紙寫「螢幕怪怪的先做這三件事」

第二項要特別強調：**一定要用 4G，不能用店裡的 Wi-Fi 測**。
用店裡 Wi-Fi 測是在測區網，那本來就會通，測了等於沒測。

---

## 關於「後台放 GitHub」這件事

方向對，但有一個要修正的地方：

> **GitHub 放的是「程式碼」和「介面」，不是「執行中的後台」。**

GitHub Pages 只能端靜態檔案，跑不了 Python、跑不了 ffmpeg，
而且它在網際網路上，看不到門市內網的那張卡。所以：

| 東西 | 放哪 | 為什麼 |
|---|---|---|
| 介面（`docs/index.html`） | GitHub Pages | 靜態檔，推一次全部門市同步更新 |
| 後台程式（`後台/*.py`） | GitHub（原始碼） | 這是更新通道，門市那台 `git pull` 就更新了 |
| **執行中的後台** | **門市的小主機** | 只有它同時看得到卡和網路 |
| 內容影片（`build/`） | 不要進 git | 每次編都不一樣，而且很大 |

「他們碰不到我的 GitHub 端」這點是對的，也做得到 ——
店員拿到的只是一個網址，改不了任何東西。

### ⚠ 上傳之前要先處理一件事

`stores.json` 裡有卡的 AP 密碼：

```json
"wifi_ap": { "ssid": "C16L-D24-000A2", "password": "88888888" }
```

那是出廠預設值。推到公開 repo 等於公告「店門口那面牆的螢幕，
密碼是 88888888」—— 走在騎樓下的人就能連上去改畫面。

兩件事都要做：

1. **把卡的 AP 密碼改掉**，不要用出廠預設
2. **`stores.json` 不要進 git**（`.gitignore` 已經擋掉了）。
   進 git 的是 `stores.範本.json`，門店資料留在各自的機器上

---

## 最後：最壞的情況到底有多壞

排一下，從壞到不壞：

| 壞掉的東西 | 螢幕 | 續碗 | 我能改內容嗎 | 怎麼辦 |
|---|---|---|---|---|
| 我家網路斷 | 正常 | 正常 | 不能 | 等 |
| 門市 4G 斷 | 正常 | **正常** | 不能 | 等電信商 |
| 小主機掛了 | **正常** | 不能 | 不能 | 請店員重開機 |
| 卡掛了 | 黑 | 不能 | 不能 | 要有人到現場 |

**只有最後一列需要人跑一趟。** 前面三列螢幕都還在播，
其中兩列連續碗都還能按。

這就是為什麼「內容在卡上、觸發在店內」這個設計值得堅持 ——
它讓大部分的故障都退化成「今天改不了東西」，而不是「門口開天窗」。
