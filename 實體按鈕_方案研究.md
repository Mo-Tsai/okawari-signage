# 續碗實體按鈕｜方案研究

> 建立：2026/08/20　｜　狀態：**已定案，見最上面的結論**
> 相關：`遠端架構.md`（觸發必須走店內）、`OKAWARI_門頭屏_系統架構_v1.0.md` §8

---

## ★ 定案（2026/08/20 晚）

繞了一圈，回到 `遠端架構.md` 原本的建議：**門市放小主機（全新 N100，約 NT$3,500–5,000）。**
它一次買回：平板觸發、遠端換內容（多店在外縣市，省兩趟高鐵就回本）、監控告警。

按鈕的部分，**自製 ESP32 按鈕擱置**——加工（焊線、組殼、每店複製）不是 Mo 的時間該花的地方。改走零加工路線：

1. **現在**：舊手機／平板鎖定觸發網頁（kiosk 模式）當按鈕——系統已做完，零新工作
2. **server.py 加冷卻**：播放中忽略連打（連打問題解在後台，跟入口用什麼無關）
3. **想要實體手感**：買成品「USB 大紅按鈕／一鍵巨集鍵盤」（一兩百塊）插小主機，
   加二十行監聽程式。不焊、不組、不設計外殼
4. **客人拍的品牌大按鈕**：留作未來項目，屆時 Mo 出設計圖、**發包**電子工作室做成品，不自己加工

下面的方案比較與 ESP32 直連卡的研究**保留**，兩個情境會用到：
（a）哪天真的不放小主機的店型；（b）發包做客人版按鈕時,給工作室的技術規格參考。

---

## 0.（2026/08/20 更新）前提改了：門市不放小主機

決定：**內容用 SD 卡灌進卡裡，卡自己按排程播，店裡沒有電腦。**
這讓下面 §2 的比較基礎整個變掉，直接講新結論：

### 三個先講死的事實

1. **iPad／手機當按鈕，不成立。** 瀏覽器講不了卡的二進位 TCP 協議
   （`遠端架構.md` 已寫過）。之前平板能按，是因為背後有筆電後台在翻譯。
   沒有後台，平板連得上卡的 Wi-Fi，但一句話都說不出口。
   而且 iPad 比小主機還貴，「省錢」的邏輯是反的。
2. **HUB 上那兩條開關線，先當作沒有。** 十之八九是「開關屏」（螢幕電源），
   不是「切節目」。就算能切，也沒有擲骰、沒有回 idle、
   **連打完全無解**（卡韌體怎麼處理連續觸發，我們控制不了）。
   問次元光電一句確認用途即可，不當主方案。
3. **ESP32 就是小主機，只是它只要 NT$150。** 卡的協議已經在
   `hd_test.py` 完整實作（TCP 10001、4 byte 表頭、XML、握手）。
   移植到 ESP32（MicroPython）之後，按鈕直連卡的 Wi-Fi 熱點送
   SwitchProgram，**不需要店內網路、不需要任何電腦**。

### 新的系統圖

```
[大按鈕] ─▶ [ESP32 · MicroPython] ──卡自己的Wi-Fi熱點──▶ [卡] ─▶ LED屏
               │  骰子、冷卻、播完切回idle 都在這裡
             USB 供電（貼標籤：請勿拔除）
```

- 擲骰（中獎 25%）、變體挑選、播放中忽略連打（或累積 combo）、
  播完 N 秒送 SwitchProgram 回 idle——原本 server.py 的觸發邏輯全部搬進韌體。
- 全店只有這一顆按鈕，狀態在它身上，**比網頁版更不怕連打**
  （沒有多台裝置同時按的競爭問題）。
- 成本：ESP32-C3 約 NT$150 + 街機按鈕 NT$100 + USB 電源 NT$100。**一店約 NT$350。**

### 要先驗證的三件事（都不用花錢）

- [ ] 筆電連卡的熱點（192.168.6.1），跑 SwitchProgram——確認 AP 模式下協議可用
- [ ] 重新發佈後節目 GUID 會不會變（決定 ESP32 是「寫死 GUID」還是「開機時查詢」）
- [ ] 卡的 AP 密碼改掉（本來就要做，按鈕上線後更是必須）

### 代價要認

- 沒有小主機 = **遠端改內容那條路沒了**，更新內容就是帶 SD 卡或筆電到場。
  這是你選 SD 卡工作流時已經接受的，按鈕不改變這件事。
- ESP32 佔用卡熱點的一個連線。若未來卡改接店內網路，ESP32 改連同一個網路即可，韌體不用重寫。

---

## 0-舊. 原本的一句話（小主機版，留著對照）

**實體按鈕的本質，是一顆會對店內小主機發一個 HTTP request 的按鈕。**
按下去 → 店內 Wi-Fi → 小主機 `/api/switch` → 卡切節目。從頭到尾不出店門，
跟 `遠端架構.md` 定下的原則完全一致。難的不是技術，是選對做法和把細節做齊。

---

## 1. 先做一個會決定一切的決策：誰來按？

這題比選硬體重要，因為兩條路做出來的東西完全不同。

| | A. 店員按（櫃檯內） | B. 客人按（櫃檯外） |
|---|---|---|
| 情境 | 客人續碗，店員順手按一下 | 客人自己拍下去，門頭屏放動畫 |
| 性質 | 操作工具 | **品牌體驗、儀式感** |
| 外觀 | 不重要，能按就好 | 非常重要，是視覺焦點 |
| 耐用度 | 一天幾十次 | 一天幾百次，會被小孩狂拍 |
| 防呆 | 店員會自律 | **必須做冷卻鎖**，不然被連打 |
| 位置 | 櫃檯內，靠近電源 | 出餐口／取碗處，電源要拉 |

我的看法：**B 才是這顆按鈕存在的理由。**
店員要觸發，平板網頁已經能按了，再做一顆實體鈕只是換個手感。
但「客人自己拍一顆大按鈕、門口的屏跟著爆動畫」是別家抄不走的記憶點，
而且外殼是 Mo 的主場——做成一顆碗、一顆飯桶、一塊要被劈的石頭都行。

可以先照 A 的規格做一顆放櫃檯試營運，確認流程順了再把它移到客人那側。
硬體是同一套，差別只在外殼和裝的位置。

---

## 2. 四條路，比一比

### 方案一 ★ ESP32 + 街機大按鈕（建議的正式版）

```
[大按鈕] ──接線──▶ [ESP32 小板] ──店內Wi-Fi──▶ [小主機:8080] ──▶ 卡
                       │
                     USB 供電（不用電池）
```

- **怎麼動**：ESP32 常駐連著店內 Wi-Fi，按下 → debounce → `GET /api/switch` →
  收到 200 亮綠燈／叫一聲，失敗亮紅燈。
- **延遲**：常駐連線下約 0.1〜0.3 秒，感覺是即時的。
  （真正的未知數還是 SwitchProgram 本身，跟按鈕無關——`_測_SwitchProgram延遲.py` 那題。）
- **成本**：ESP32-C3 開發板約 NT$100〜200、60mm 街機圓頂按鈕 NT$50〜150、
  USB 電源 NT$100。**一店一顆 NT$300〜500**，蝦皮和電子材料行都有。
- **韌體**：建議用 **ESPHome** 而不是手刻 Arduino——Wi-Fi 設定有現成的
  配網頁面（新店開通不用重燒）、支援 OTA 更新、`http_request` 是內建動作。
  設定檔大概三十行，Claude 寫。
- **外殼**：3D 列印或現有模型改。這是整個方案裡唯一「只有 Mo 能做」的部分。

| 優 | 劣 |
|---|---|
| 便宜、可複製、出貨工具邏輯照搬（燒 Wi-Fi + 店號） | 要焊兩條線（按鈕到板子，很簡單） |
| 按鈕手感、大小、外觀完全自選 | 自己維護韌體（但 ESPHome 很穩） |
| 放哪都行，只要有 USB 電 | |

### 方案二：USB 按鈕直接插小主機（最快能動的版本）

按鈕偽裝成鍵盤（市售「USB 大紅按鈕」或一鍵巨集鍵盤，NT$150〜600），
插在小主機上，小主機跑一支小程式：聽到那顆鍵 → 打自己的 `/api/switch`。

| 優 | 劣 |
|---|---|
| **零無線、零電池、零韌體**，最不會壞 | 被 USB 線綁死（被動線 5m 上限） |
| 今天買明天能動 | 按鈕必須在小主機旁邊 |
| 斷 Wi-Fi 也照樣動 | 客人按的情境幾乎不可行（線拉不過去） |

**適合當第一步**：如果小主機本來就放櫃檯附近，先用這個把流程跑起來，
一邊等正式版的外殼做好。監聽程式二十行，Claude 寫。

### 方案三：市售 Wi-Fi 智慧按鈕（Shelly Button 之類）

Shelly 這類產品支援「本地 webhook」——按下去直接打區網 URL，不經雲端，理論上合用。

| 優 | 劣 |
|---|---|
| 免焊接、免韌體 | **電池版有深度睡眠，按下去要先醒來連 Wi-Fi，延遲 1〜3 秒**——客人站在旁邊的場景不能接受 |
| 產品化，外觀整齊 | 插電常駐才快，那成本和 ESP32 差不多但按鈕小顆、不能改外觀 |
| | Aqara/IKEA 那類 Zigbee 鈕還要多一顆網關，違反「東西越少越好」 |

**結論：不推。** 它省下的功夫（韌體）我們本來就出得起，付出的代價（延遲、外觀）剛好都是這案子在乎的。

### 方案四：接在卡的硬體輸入上（先當作沒有）

有些 LED 控制卡有感測器／繼電器腳位。就算灰度這張有，也不該用：
擲骰（中獎 25%）、變體挑選、播完自動回 idle 全在後台，
直接接卡等於繞過大腦只剩脊椎反射。**唯一的用途**是哪天想做
「小主機掛了按鈕還要能動」的終極備援，再回來研究。
（順帶：問次元光電時可以順口問一句卡有沒有 IO 腳位，記著就好。）

---

## 3. 建議的路線

```
第一步（現在）   方案二 USB 按鈕接筆電，把「實體按下去→屏有反應」跑通
                  同時測 SwitchProgram 延遲（本來就要測）
第二步（外殼）   Mo 設計按鈕外殼；Claude 寫 ESPHome 韌體，辦公室用測試卡調到穩
第三步（裝機）   正式版 = 方案一。裝機檢查表加一條：按鈕按下去會動、紅燈情境教過店員
之後（如果）     客人版：把按鈕移到出餐口，外殼換成給客人拍的版本
```

---

## 4. 不管選哪條路都要做的四件事

### 一、後台要加「冷卻」

現在 `/api/switch` 是來一發吃一發。實體按鈕（尤其客人按的）一定會被連打，
要在後台擋：**動畫播放中再按 → 不重切**。兩種擋法，順序上先做甲：

- **甲（先做）**：播放期間的按壓直接忽略，回一個「播放中」讓按鈕亮黃燈。
- **乙（有趣，之後做）**：播放期間的按壓**累積計數**，播完接一段「多人版」——
  這剛好就是系統架構 §8 寫過的累積結算式，變成 combo 機制反而加分。
  代價是 Mo 要多畫「多人版」動畫。

### 二、按鈕要有回饋

按了沒反應是最傷的。門頭屏在店外面、按的人在店裡面，看不到屏，所以回饋要在按鈕上：

- 按下 → 立刻亮（讓人知道有按到）
- 後台回 200 → 綠燈／短嗶
- 失敗或逾時 → 紅燈 → 店員知道改用平板（備援本來就有）

### 三、供電與網路的落地細節

- **一律插電，不用電池。** 電池是延遲和沒電巡檢兩個問題的來源。
- 插頭照小主機的規矩貼標籤：「請勿拔除 · 續碗按鈕」。
- 小主機在店內路由器上**固定 IP**（或 mDNS），按鈕韌體裡才有東西可指。
- 新店開通：出貨工具的邏輯照搬——燒好 Wi-Fi 帳密 + 小主機位址 + store_id 再寄出。

### 四、裝機檢查表加兩條

- [ ] 實體按鈕按下去，屏在一秒內有反應（用碼錶感受，不用真的量）
- [ ] 拔掉店的對外網路再按一次，**照樣要動**（它走的是店內，本來就該動）

---

## 5. 風險與備援

| 壞掉的東西 | 續碗還能按嗎 | 怎麼辦 |
|---|---|---|
| 按鈕掛了／被拔 | 能，用平板網頁 | 原本的路還在，按鈕只是多一條 |
| 店內 Wi-Fi 掛了 | 按鈕不能，平板也不能 | 這是本來就有的風險，不是按鈕新增的 |
| 小主機掛了 | 不能 | 同 `遠端架構.md`：請店員重開機 |

**按鈕是加法，不動任何現有的路。** 做壞了大不了拆掉，系統回到今天的樣子。

---

## 6. 待決事項

- [ ] 誰來按：店員版先行、客人版為目標？（§1，建議先 A 後 B）
- [ ] 冷卻方式：先做「播放中忽略」，combo 累積版要不要排進 Mo 的動畫工作量？
- [ ] 按鈕外殼：形狀、尺寸、印刷（Mo 的主場）
- [ ] 一店幾顆？（櫃檯一顆？出餐口一顆？）
- [ ] 問次元光電：卡有沒有 IO 輸入腳位（只是記著，不影響方案）
